-Interview teachers about how to improve the program
    Notes for the class
    Create a button to show the week or the day in call list
    Create a button for ‘todos presentes’
    Eliminate the absent buttons and create a button to present and absent, like the play in youtube
-Verify if the objects existe in functions
-Use CRUD
-Fix get_student_by_attendance function
-Fix the 3 get_by function returning the row
-Use python docstring in all the functions 
-create a file called db connection that use function to conect the csvs and python files
-create function for repeated things, like objects, vars, etc...